{#it's mean in layout template#}
{% extends template %}
{% block before_content %}
    <div class="row">
        <div class="col-sm-12">
            <h1 class="heading-h1">{{ heading_title }}</h1>
        </div>
    </div>
{% endblock %}
{% block content %}
    <div class="register-page">
        <p>{{ text_account_already }}</p>
        <form action="{{ action }}" method="post" enctype="multipart/form-data" class="form-horizontal">
            <fieldset id="account">
                <h3>{{ text_your_details }}</h3>
                <div class="">

                {% include partial.d_account_field.template with partial.d_account_field|merge({form:''}) %}
                {% for custom_field in custom_fields %}
                    {% if custom_field.location == 'account' %}
                        {% set custom_field = custom_field|merge({form:'inline'}) %}
                        {% include partial.d_custom_field.template with partial.d_custom_field|merge(custom_field) %}
                    {% endif %}
                {% endfor %}
                </div>

            </fieldset>
            {% if constant('VERSION') < '3.0.0.0' %}
                <fieldset id="address">
                    <h3>{{ text_your_address }}</h3>
                    {% include partial.d_address_field.template with partial.d_address_field|merge({form:'inline'})  %}
                    {% for custom_field in custom_fields %}
                        {% if custom_field.location == 'address' %}
                            {% set custom_field = custom_field|merge({form:'inline'}) %}
                            {% include partial.d_custom_field.template with partial.d_custom_field|merge(custom_field) %}
                        {% endif %}
                    {% endfor %}
                </fieldset>
            {% endif %}
            <fieldset>
                <h3>{{ text_your_password }}</h3>
                <div class="form-group required">
                    <label class="field-label label-right-md"
                           for="input-password"><span>{{ entry_password }}</span></label>
                    <div class="">
                        <input type="password" name="password" value="{{ password }}"
                               id="input-password" class="field form-control"/>
                        {% if error_password %}
                            <div class="text-danger">{{ error_password }}</div>
                        {% endif %}
                    </div>
                </div>
                <div class="form-group required">
                    <label class="field-label label-right-md"
                           for="input-confirm"><span>{{ entry_confirm }}</span></label>
                    <div class="">
                        <input type="password" name="confirm" value="{{ confirm }}"
                               id="input-confirm" class="field form-control"/>
                        {% if error_confirm %}
                            <div class="text-danger">{{ error_confirm }}</div>
                        {% endif %}
                    </div>
                </div>
            </fieldset>
            <fieldset>
                <h3>{{ text_newsletter }}</h3>
                <div class="form-group">
                    <div class="">
                        <label class="field-label radio-label">{{ entry_newsletter }}</label>
                    </div>
                    <div class="">
                        <span class="custom-control custom-radio">
                            <input type="radio" name="newsletter" value="1" class="custom-control-input"  {% if newsletter %}checked="checked"{% endif %}>
                            <label class="custom-control-label" for="customRadio1">{{ text_yes }}</label>
                        </span>
                        <span class="custom-control custom-radio">
                            <input type="radio" name="newsletter" value="0" class="custom-control-input"  {% if not newsletter %}checked="checked"{% endif %}>
                            <label class="custom-control-label" for="customRadio1">{{ text_no }}</label>
                        </span>
                    </div>
                </div>
            </fieldset>
            {{ captcha }}
            {% if text_agree %}
                <div class="buttons clearfix">
                    <div class="pull-right">
                        <label class="field-label checkbox-label">
                            {{ text_agree }}
                            <input type="checkbox" name="agree" value="1" {% if agree %}checked="checked"{% endif %} />
                        </label>
                        &nbsp;
                        <input type="submit" value="{{ button_continue }}" class="btn btn-primary btn-shrink"/>
                    </div>
                </div>
            {% else %}
                <div class="buttons clearfix">
                    <div class="pull-right">
                        <input type="submit" value="{{ button_continue }}" class="btn btn-primary "/>
                    </div>
                </div>
            {% endif %}
        </form>


        <script type="text/javascript"><!--
            $(function () {
                d_address_field.init({
                    zone_id: '{{ zone_id }}',
                    text_none: '{{ text_none }}',
                    text_select: '{{ text_select }}'
                });
                d_custom_field.init();
            })

            //--></script>
    </div>

{% endblock %}